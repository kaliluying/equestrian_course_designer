# 应用环境变量示例文件
# 创建实际的.env文件时，请复制此文件并根据您的环境进行修改

# 应用环境
# 有效值: development, production, test, staging
# NODE_ENV=development

# # API服务器地址
# # 开发环境通常为本地地址
# VITE_API_HOST=localhost:8000

# # 前端应用地址（用于生成邀请链接等）
# VITE_APP_URL=http://localhost:5173

# # WebSocket连接超时设置(毫秒)
# VITE_WS_CONNECT_TIMEOUT=10000

# # WebSocket断开重连间隔(毫秒)
# VITE_WS_RECONNECT_INTERVAL=2000

# # WebSocket最大重连尝试次数
# VITE_WS_MAX_RECONNECT_ATTEMPTS=5

# # 是否在控制台输出调试信息
# VITE_DEBUG=true

# # 服务端口号（默认为5173，如果遇到端口占用或权限问题，可以修改）
# PORT=5173

# # 是否启用HTTPS（本地开发通常不需要）
# VITE_ENABLE_HTTPS=false

# ----- 生产环境配置示例 -----
NODE_ENV=production
VITE_API_HOST=equestrian.top
VITE_APP_URL=http://equestrian.top
VITE_WS_CONNECT_TIMEOUT=10000
VITE_WS_RECONNECT_INTERVAL=5000
VITE_WS_MAX_RECONNECT_ATTEMPTS=3
VITE_DEBUG=false
PORT=80
VITE_ENABLE_HTTPS=true 

# ----- 预发布环境配置示例 -----
# NODE_ENV=staging
# VITE_API_HOST=staging-api.yourdomain.com
# VITE_APP_URL=https://staging.yourdomain.com
# VITE_WS_CONNECT_TIMEOUT=10000
# VITE_WS_RECONNECT_INTERVAL=3000
# VITE_WS_MAX_RECONNECT_ATTEMPTS=3
# VITE_DEBUG=true
# PORT=8080
# VITE_ENABLE_HTTPS=true 